#SPDX-License-Identifier: MIT-0
---
# defaults file for roles/private-registry

registry_hostname: "registry.local"
registry_port: "8000"
registry_ip: "{{ ansible_default_ipv4.address }}"
registry_dir: "/opt/private-registry" # Thư mục chứa docker-compose.yml
