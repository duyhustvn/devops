services:
  registry:
    image: registry:2
    container_name: private-registry
    restart: always
    ports:
      - "{{ registry_port }}:5000"
    volumes:
      - registry-data:/var/lib/registry

volumes:
  registry-data:
    name: registry-data
